# 数据导入工具环境配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# 数据库文件存储根目录
IMPORT_DBROOT=./db

# 报表输出目录
RPTROOT=./rpt

# 数据类型映射文件路径
IMPORT_TYPE_YAML=./config/type.yaml

# PostgreSQL连接字符串（用于映射表导出）
# 格式: postgres://用户名:密码@主机:端口/数据库名?参数
# 示例: PGDSN=postgres://myuser:mypassword@localhost:5432/mapping_db?sslmode=disable
PGDSN=

# 加密密钥文件路径（可选，默认为 ./db/encrypt.key）
ENCRYPT_KEY_PATH=

# 映射数据库文件路径（可选，默认为 ./db/mapping.duckdb）
ENCRYPT_MAPPING_DB=

# PostgreSQL密码加密专用密钥路径（可选，默认为 ./config/pg_encrypt.key）
PG_ENCRYPT_KEY_PATH=

# 数据导入基础目录（可选）
IMPORT_BASEDIR=
